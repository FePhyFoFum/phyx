# this might not be necesary (might be root)
#sudo: required

language: cpp

os:
- linux

# default is xenial
dist: bionic

addons:
  apt:
    sources:
      - sourceline: "ppa:ubuntu-toolchain-r/test"

compiler:
  - gcc

### how to install these (and maybe cache for a faster build?)
# some (many?) may already be available
before_install:
  - sudo apt-get update
  - sudo apt-get install -y autotools-dev
  - sudo apt-get install -y autoconf
  - sudo apt-get install -y automake
  - sudo apt-get install -y libtool
  - sudo apt-get install -y liblapack-dev
  - sudo apt-get install -y libatlas-cpp-0.6-dev
  - sudo apt-get install -y libnlopt0
  - sudo apt-get install -y libnlopt-dev
  - sudo apt-get install -y libarmadillo-dev
  - sudo apt-get install -y libnlopt-cxx-dev

# should we include autoconf too?
script:
  - cd src && autoreconf -fi && ./configure && make -j && make test

